services:
  carebridge:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
      CAREBRIDGE_API_URL: http://127.0.0.1:3000
      BOT_ENABLED: "true"
    restart: unless-stopped
